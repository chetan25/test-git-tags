pipeline {
   agent any

   stages {
        stage("build") {
            steps {
                echo "Testing this Jenkins"
            }
        }
   }

   post {
        always {

        }
        success {
            echo "Successfully done"
        }
   } 
}